---
title: Enhancing Graph Neural Networks Using Proximity Graphs for Dust Source Emission Forecasting
title_zh: 利用近邻图增强图神经网络用于沙尘源排放预测
authors: "Maryam Sanisales, Zahed Rahmati, Ali Darvishi Boloorani, Ali Vefghi"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19825v1"
tags: ["query:aqac"]
score: 8.0
evidence: 利用图神经网络作为代理模型进行沙尘源排放预测，直接应用于空气质量模型
tldr: 沙尘暴造成严重环境与健康危害，传统预测方法难以捕捉其复杂时空动态。本文提出使用Delaunay三角剖分、Gabriel图等邻近图作为图神经网络（GNN）的输入图结构，通过消息传递建模空间邻近关系。实验表明，基于邻近图的GNN在沙尘源排放预测中显著优于使用随机图的GNN和LSTM模型。该方法证明了邻近图能增强GNN对时空数据的建模能力，为沙尘预报提供了有效且鲁棒的工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19825v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19825v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1666, \"height\": 839, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19825v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1341, \"height\": 1713, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19825v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1240, \"height\": 1325, \"label\": \"Table\"}]"
motivation: 传统方法难以捕捉沙尘源排放的复杂时空动态，亟需能有效建模空间邻近关系的预测模型。
method: 使用Delaunay三角剖分、Gabriel图、k近邻图和Yao图作为GNN（GraphSAGE、GCN、GAT）的输入图结构进行消息传递。
result: 基于邻近图的GNN在预测精度上显著优于使用随机图的GNN和LSTM模型。
conclusion: 邻近图能有效增强GNN捕捉空间关系的能力，为沙尘源排放预报提供了强大工具。
---

## 摘要
准确预测沙尘源排放对于减轻沙尘暴带来的重大环境和健康危害至关重要。传统预测方法往往难以捕捉这些现象的复杂时空动态。本文证明，近邻图使图神经网络（GNN）能够有效建模数据点之间复杂的空间和时间关系。具体而言，我们使用近邻图（如德劳内三角剖分、加布里埃尔图、k近邻图和姚图）作为GNN（包括GraphSAGE、图卷积网络和图注意力网络）的输入进行消息传递。我们的方法强调了将近邻图与GNN集成用于稳健准确的沙尘源预测的有效性。为了突出近邻图表示的重要性，我们将我们的方法与使用随机图进行消息传递的GNN进行了比较。结果表明，使用近邻图的GNN显著优于使用随机图的GNN，并且在沙尘源排放预测方面也远优于长短期记忆（LSTM）模型。

## Abstract
Accurate prediction of dust source emissions is critical for mitigating the significant environmental and health hazards posed by dust storms. Traditional forecasting methods often struggle to capture the complex spatiotemporal dynamics of these phenomena.   In this paper, we demonstrate that proximity graphs enable Graph Neural Networks (GNNs) to effectively model the intricate spatial and temporal relationships between data points. Specifically, we use proximity graphs--such as Delaunay triangulation, Gabriel graph, k-Nearest Neighbor graph, and Yao graph--as the input for GNNs (including GraphSAGE, Graph Convolutional Networks, and Graph Attention Networks) to perform message passing.   Our approach highlights the effectiveness of integrating proximity graphs with GNNs for robust and accurate dust source forecasting. To emphasize the importance of proximity graph representations, we compare our method against GNNs using random graphs for message passing. The results show that GNNs with proximity graphs significantly outperform those with random graphs and are also far superior to Long Short-Term Memory (LSTM) model in dust source emission forecasting.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
沙尘暴对生态环境、人类健康（呼吸与心血管疾病）、农业、交通和能源（如太阳能面板效率）造成严重危害。准确预测沙尘源排放对于制定缓解措施至关重要，但传统方法（如支持向量机、决策树、线性回归、LSTM等）难以同时捕捉沙尘排放中复杂的空间依赖（如地理邻近性）和时间动态（如季节性变化）。现有研究表明，图神经网络（GNN）能够建模实体间的图结构关系，但远程遥感中的沙尘数据通常是表格形式，缺乏预定义的图结构。因此，**核心问题**是如何为GNN构造有效的图结构以表达沙尘源之间真实的空间邻近关系，从而提升预测精度。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：利用计算几何中的**近邻图**（Proximity Graphs）将每个时间步的沙尘源空间点集转化为图，节点代表沙尘源，边表示空间邻近关系。然后使用静态GNN（GraphSAGE、GCN、GAT）在这些图上进行消息传递，实现归纳式学习。
- **关键技术细节**：
  - **图构造**：对每个月的数据点集（约11000个点）分别构造以下近邻图：
    - Delaunay三角剖分（保证三角形的内角最大，避免狭长三角形）
    - Gabriel图（边连接的圆盘内无其他点，是Delaunay的子图，更稀疏）
    - k-NN图（k=1,2,3）
    - Yao图（将空间划分为r个等角扇形，每个扇形内连最近邻，r=2,4,6）
  - **时间图连接**：将连续月份的图连接为统一图。不同于随机连接，采用**空间近邻**方式：对当月每个节点，在下一月图中找到k个最近空间邻居并连边（k=3），从而保持时空结构。
  - **GNN模型**：
    - **GCN**：通过归一化邻接矩阵聚合邻居特征（公式1）。
    - **GAT**：注意力机制（公式2-4）学习邻居的重要性权重，支持归纳学习。
    - **GraphSAGE**：采样邻居并聚合（均值、LSTM、池化），适合归纳学习（公式5-6）。
  - **学习范式**：归纳学习（训练集、验证集、测试集按时间划分，互不重叠），训练图240个月，验证12个月，测试12个月。
  - **分类任务**：二分类（预测未来是否活跃），处理严重类别不平衡（负样本远多于正样本），采用空间采样策略（仅保留离正样本最近的1个负样本）和上采样。

## 3. 实验设计
- **数据集**：Tigris and Euphrates流域（TEB），2000-2021年共22年月度数据，总计11,618个独特沙尘源（10,422用于训练，2021年数据用于测试）。每个沙尘源含11个环境特征（植被、降水、土壤湿度、风速、土壤温度、土壤质地、土壤厚度、海拔、坡度等）。
- **基准方法**：
  - **随机图**（与Delaunay边数相同的随机连接）
  - **LSTM**（双向，2层，隐藏层64-256，序列长度6）
- **对比的GNN变体**：GraphSAGE / GCN / GAT × 8种近邻图（Delaunay, Gabriel, Yao2/4/6, 1-NN/2-NN/3-NN），每种配置使用3-NN时间连接。
- **评价指标**：Accuracy (ACC), AUC, Precision, Recall。
- **训练设置**：超参数搜索（隐藏通道8-512，学习率0.001-0.1，epochs 500-2000，早停patience 100-200），优化器Adam，损失函数BCEWithLogitsLoss。每个配置独立重复50次（不同初始化）。
- **数据子集**：分别评估了降采样和上采样后的数据。

## 4. 资源与算力
文中**未明确说明**使用的GPU型号、数量、训练时长等硬件资源。仅提到超参数搜索空间和重复次数（50次），未估算总计算量。

## 5. 实验数量与充分性
- **实验数量**：共测试了3种GNN × 8种近邻图 + LSTM + 随机图，合计约3×8+2=26种主要配置。每种配置重复50次，总计约1300次实验。
- **充分性**：实验较为充分。对比了多种近邻图（覆盖不同几何特性）和随机图基线，并包含传统时序模型LSTM。所有实验使用相同的时间连接方式和评价指标，公平。
- **客观性**：用50次重复降低随机初始化影响，报告了不同配置下的多个指标，未仅挑最好结果。但未进行统计显著性检验（如t检验）明确差异是否显著。

## 6. 论文的主要结论与发现
- **近邻图GNN显著优于随机图和LSTM**：所有近邻图GNN在AUC/ACC上均高于随机图（随机图AUC约0.60-0.62，近邻图最好达0.72-0.73）和LSTM（AUC 0.52）。
- **图拓扑结构影响性能**：
  - Delaunay图在GraphSAGE和GAT中整体表现最均衡（AUC 0.72-0.73）。
  - 1-NN图在GCN中Recall最高（0.96），但Precision低；Yao4/6图获得高Precision（0.77）。
  - k-NN图的性能随k值增加而改善，但3-NN在图稀疏性上平衡较好。
- **归纳学习有效**：模型在未见过的未来月份上成功泛化。
- **LSTM表现最差**，说明单纯时序模型无法捕捉空间结构。

## 7. 优点
- **方法创新**：首次将多种近邻图（Delaunay、Gabriel、Yao等）引入GNN用于沙尘排放预测，系统比较了不同图拓扑对预测性能的影响。
- **解决关键挑战**：针对表格数据无预定义图结构的问题，提供了基于空间邻近性的图构造方案，且设计了时间图连接方法（空间近邻而非随机）以保持时空连续性。
- **实验设计严谨**：使用重复实验（50次）降低方差；对比随机图基线证明近邻图有效；涵盖多种GNN架构和评价指标。
- **实际意义强**：为沙尘暴预警提供了可操作的预测工具，且方法可推广到其他空间驱动的环境过程（如城市空气污染、野火蔓延）。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅使用单一流域（TEB）数据，未在多个地理位置或气候区验证泛化性。
  - 时间分辨率仅月度，更高频率（如日或小时）的预测能力未知。
  - 对比方法有限：未与近年先进的时空预测模型（如STGCN、LSTM-GCN混合模型）比较。
- **偏差风险**：
  - 类别不平衡处理（仅保留最近负样本）可能引入采样偏差，影响模型对远处负样本的识别。
  - 时间连接固定为k=3，未探索不同k值的影响。
- **应用限制**：
  - 图构造依赖精确的空间坐标和完整的数据集，实际部署中可能因数据缺失或噪声影响图质量。
  - 计算成本：近邻图构造（尤其Delaunay/Yao）在大规模点集上可能开销较大，论文未分析时间复杂度。
- **其他**：未提供模型可解释性分析（哪些环境特征重要）；未讨论不确定性量化。

（完）
