---
title: Fourier Neural Operators for Rayleigh-Bénard Convection
title_zh: 瑞利-贝纳尔对流的傅里叶神经算子
authors: "Chelsea Maria John, Thibaut Lunet, Sebastian Götschel, Andreas Herten, Stefan Kesselheim, Daniel Ruprecht"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02088v1"
tags: ["query:aqac"]
score: 9.0
evidence: 傅里叶神经算子用于对流建模，属于神经算子代理模型
tldr: 针对Rayleigh-Bénard对流建模，提出改进的傅里叶神经算子，通过预测时间增量而非完整解，提高了精度。模型仅314k参数、1.26MB，推理仅7ms，性能优于标准FNO。尽管FNO可泛化至更细网格，但精度受限于训练数据分辨率。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02088v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 693, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02088v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1111, \"height\": 1011, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02088v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1309, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02088v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1268, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02088v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 756, \"height\": 405, \"label\": \"Table\"}]"
motivation: 标准FNO在Rayleigh-Bénard对流建模中精度有限，需更高效准确的替代方案。
method: 采用改进FNO预测时间增量，而非直接预测完整解。
result: 模型参数314k，大小1.26MB，推理耗时7ms，精度优于标准FNO基线。
conclusion: FNO可泛化至更细网格，但精度受训练数据分辨率限制。
---

## 摘要
我们提出了一种改进的傅里叶神经算子（FNO），用于模拟二维瑞利-贝纳尔对流，通过预测时间增量而非完整解，相比标准FNO基线实现了更高精度。得到的模型紧凑（314k参数，1.26 MB）且快速（推理时间7毫秒），同时保持了与先前基准测试相当的精度。我们表明，尽管FNO可以推广到更细的网格，但精度仍受限于训练数据的分辨率。

## Abstract
We propose an improved Fourier Neural Operator (FNO) for modeling two-dimensional Rayleigh-Bénard convection by predicting time increments instead of full solutions, achieving higher accuracy than a standard FNO baseline. The resulting model is compact (314k parameters, 1.26 MB) and fast (7 ms inference), while maintaining similar accuracy as demonstrated in previous benchmarks. We show that although FNOs generalize to finer meshes, accuracy remains limited by the resolution of the training data.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：湍流对流（如Rayleigh-Bénard对流）的高分辨率数值模拟计算成本极高，传统网格求解器在强湍流（高Ra数）下难以高效运行。论文旨在利用傅里叶神经算子（FNO）构建快速、紧凑的代理模型，替代昂贵的全阶数值求解器。
- **整体含义**：提出一种改进的FNO架构，通过**预测时间增量**而非完整解，实现更高精度、更小模型尺寸和更快推理速度。该模型适用于时空并行方法（如Parareal）中的粗传播器，在保持湍流统计特性的同时显著降低计算负担。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将FNO训练为**数据驱动的一步时间积分器**——学习从状态 \( U(t) \) 到增量 \( \Delta U = \Delta t^{-1}(U(t+\Delta t)-U(t)) \) 的映射，再通过 \( U(t+\Delta t) = U(t) + \Delta t \cdot \mathcal{O}(U(t)) \) 恢复解。相比直接预测完整解，增量预测显著降低噪声和误差。
- **关键技术细节**：
  - 架构：两层Fourier层，12个Fourier模数，采用GELU激活函数，Adam优化器，余弦学习率调度。
  - **提升/投影层（P和Q）**：使用多层1D卷积（宽度=4×投影通道维度）替代单层线性层，以更好捕获局部特征。
  - 模型参数：314,772个（约1.26 MB），推理时间7 ms（batch size=1）。
- **学习目标**：对比“解目标”（直接预测 \( U(t+\Delta t) \)）与“增量目标”（预测 \( \Delta U \)），两者均使用相对L2损失。评估时统一用重构解的相对误差。

## 3. 实验设计：数据集、基准、对比方法
- **数据集生成**：
  - 使用Dedalus谱方法求解器生成训练/验证数据，网格256×64，时间步 \( \Delta t = 10^{-3} \)，Ra=10^7，Pr=1，边界温度 \( \theta_H=1, \theta_C=0 \)。
  - 模拟从随机扰动开始，运行至 \( T_{init}=100 \) 达到伪稳态，收集 \( T=100 \) 至200之间的数据，共2000个样本（每步记录 \( u, w, \theta, p \)），80%训练，20%验证。
- **基准与对比方法**：
  - 基线模型：标准FNO（解目标，线性提升/投影层，StepLR调度）。
  - 改进模型：增量目标+1D卷积层+余弦LR。
  - 与**Straat et al. (2025)** 的3D FNO对比：相同设置（Ra=5×10^6, Pr=0.7, 网格96×64），但Straat模型使用8层Fourier层、64投影通道、16模数，参数约33M（132 MB），推理0.45 s；本文模型仅33M参数、10-30 ms推理。
- **消融实验**：比较解目标与增量目标、不同提升/投影层设计、不同时间步长（\( \Delta t = 1, 0.1, 0.01, 0.001 \)）。

## 4. 资源与算力
- **训练硬件**：单张NVIDIA A100（40 GB）GPU（JURECA-DC超级计算机）。
- **训练时长**：改进模型训练11,500 epochs，耗时约11小时。
- **对比实验（Straat基准）**：使用8张NVIDIA A100 GPU，训练500 epochs，耗时3.5小时。
- **推理耗时**：改进模型7 ms（batch size=1）；Straat基准：10-30 ms（batch size=10-50）。

## 5. 实验数量与充分性
- **实验组数**：
  - 主实验（表2）：改进模型 vs 基线，在200个验证样本上比较各变量误差。
  - 时间泛化实验（表3）：5种时间步长（1,0.1,0.01,0.001）× 每种自回归步数（1000,100,10,1），共4组。
  - 分辨率不变性实验（表4）：训练网格256×64，测试网格包括64×64, 256×32, 64×32, 512×128，评估5种分辨率+插值对比。
  - 与Straat对比实验（表5）：单步误差（t=0.5）和30秒滚动误差。
- **充分性评价**：
  - **充分**：覆盖了核心对比、消融（学习目标、架构组件）、时间/空间泛化、与现有工作复现对比。
  - **公平性**：对比基线时采用相同训练数据和epoch数；与Straat对比采用相同物理设置（Ra, Pr, 网格），但模型架构差异较大（2D vs 3D，参数数量悬殊），结论中明确指出了trade-off。

## 6. 论文的主要结论与发现
1. **增量预测优于解预测**：增量目标的平均相对误差（5.8e-5）比解目标（1.4e-3）低两个数量级，且优于恒等基线（1.4e-4），证明模型学到有效动态。
2. **架构改进有效**：1D卷积提升/投影层+余弦LR调度显著提升精度（平均误差从1.4e-3降至2.1e-5）。
3. **FNO具有分辨率不变性**：可泛化到未见过网格（如512×128），误差保持在相同量级（~1e-4），但精度受限于训练数据分辨率，更细网格不自动提升精度。
4. **与Straat模型对比**：本文模型更紧凑（314k vs 33M参数）、推理更快，但长时间自回归滚动误差累积更快（30秒后平均误差0.11 vs 0.04）。适合作为粗传播器，而非高精度长期预测。
5. **时间步长影响**：模型在较小时间步长（如0.001）上表现良好，但自回归滚动存在轻微误差累积。

## 7. 优点
- **方法创新**：将FNO训练为增量预测器，相当于数据驱动的时间积分器，显著降低噪声、提升稳定性。
- **模型高效**：仅314k参数、1.26 MB，推理7 ms，适合嵌入迭代算法（如Parareal）。
- **实验全面**：涵盖消融、时间/空间泛化、与现有方法复现对比，结论有数据支撑。
- **开源透明**：提供训练数据、代码链接（GitHub、HuggingFace）。

## 8. 不足与局限
- **长时间滚动误差累积**：与3D FNO（Straat）相比，本文模型在60步自回归后误差增长更快，不适合直接作为高精度长期预测器。
- **分辨率上界**：FNO精度受限于训练数据分辨率，无法像数值求解器一样通过细化网格提升精度，限制了在超高分辨率场景的应用。
- **实验覆盖有限**：
  - 仅测试了单个Ra数（10^7）和Pr数（1），未系统评估不同参数下的泛化性。
  - 时间步长实验仅测试了四个离散值，未探索自适应步长策略。
  - 与Straat对比时，模型架构差异大（2D vs 3D），且Straat论文未提供各分量误差，只能比较平均损失，公平性存疑。
- **偏差风险**：训练数据来自Dedalus模拟（谱方法），未与真实实验对比；数据仅在单一初始条件（随机扰动）上生成，可能缺乏多样性。
- **应用限制**：模型仅针对2D RBC，推广到3D或更复杂几何需重新训练；作为粗传播器时，误差累积速率需进一步分析与控制。

（完）
