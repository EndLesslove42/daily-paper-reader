---
title: Approximate Dynamic Optimization via Deep Neural Operators
title_zh: 基于深度神经算子的近似动态优化
authors: "Amin Nassaji, Ilias Mitrai, Prodromos Daoutidis"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03861v1"
tags: ["query:aqac"]
score: 8.0
evidence: 深度神经算子作为动态优化代理模型，可直接应用于空气质量代理建模
tldr: 化工过程动态优化涉及求解最优输入曲线，传统方法计算负担重。本文采用深度神经算子（DeepONet和傅里叶增强DeepONet）作为替代求解器，在批次聚合反应器案例中优化温度曲线以满足分子量分布和转化率目标。结果表明傅里叶增强DeepONet预测误差低于标准DeepONet和前馈网络，为高效求解动态优化问题提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03861v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1092, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03861v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 867, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03861v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 1576, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03861v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 936, \"height\": 526, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03861v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 633, \"height\": 229, \"label\": \"Table\"}]"
motivation: 传统动态优化方法计算成本高，难以在线应用，需要更高效的近似求解方法。
method: 采用DeepONet和傅里叶增强DeepONet逼近从工况参数到最优温度曲线的映射关系。
result: "在批次聚合反应器中，傅里叶增强DeepONet的预测误差比标准DeepONet和前馈网络低50%以上。"
conclusion: 深度神经算子能高效近似动态优化解，傅里叶增强结构显著提升精度与泛化能力。
---

## 摘要
本文研究了非线性动态优化问题的求解，该问题通过计算最优操作输入曲线来实现期望的输出曲线。此类轨迹优化问题常见于化工过程应用，例如，在批处理过程中，通过计算最优温度或进料曲线（针对补料分批过程）来控制随时间变化的产品质量曲线，从而严格控制反应速率或放热速率。我们提出了深度神经算子来近似函数到函数的映射，作为此类动态优化问题求解的替代模型。我们特别在批处理聚合反应器案例中使用了深度算子网络和傅里叶增强深度算子网络，通过最优温度程序来控制数均分子量和重均分子量曲线以及最终转化率目标。结果表明，傅里叶增强深度算子网络在近似不同实例的动态优化问题解方面表现良好，与标准深度算子网络和标准前馈神经网络相比，预测误差更低。

## Abstract
This paper addresses the solution of nonlinear dynamic optimization problems that compute optimal manipulated input profiles to enforce desired output profiles. Such trajectory optimization problems commonly arise in chemical process applications, for example, batch processes where optimal temperature or feeding profiles (in case of fed-batch processes) are calculated to enforce time-varying product quality profiles, tightly controlling the reaction rate or rate of heat generation. We propose deep neural operators that approximate function to function mappings as surrogates for the solution of such dynamic optimization problems. We specifically employ deep operator networks (DeepONets) and Fourier-enhanced DeepONets in a batch polymerization reactor case study for which number-average and weight-average molecular weight profiles, together with a final conversion target, are enforced through an optimal temperature program. Our results show that the Fourier-enhanced DeepONet architecture performs very well in approximating the solution of the dynamic optimization problem for different instances, achieving a lower prediction error compared to the standard DeepONet architecture and standard feedforward neural networks.

---

## 论文详细总结（自动生成）

# 基于深度神经算子的近似动态优化：详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：非线性动态优化（DO）问题，特别是轨迹优化问题，在化工过程（如批处理聚合反应器）中广泛存在。此类问题需要为每个新的工况实例（不同的期望输出曲线、初始条件、时间范围等）从头求解最优控制输入，计算负担沉重，难以用于在线决策或大规模研究。
- **研究动机**：已有工作多将神经算子用作系统模拟器或逆映射嵌入优化器，但本文提出**直接学习从问题规格（期望轨迹、标量参数）到最优控制轨迹的算子映射**，从而避免反复求解完整优化问题。
- **整体含义**：通过数据驱动的算子学习方法，实现动态优化问题的高效近似求解，为化工批处理过程的实时控制与操作优化提供新途径。

## 2. 论文提出的方法论

- **核心思想**：将动态优化问题的解视为一个算子 G，它将输入函数（如期望的分子量曲线 Mn(t), Mw(t) 和最终转化率 X(Tf)）映射到最优输入函数（温度轨迹 T*(t)）。使用深度算子网络（DeepONet）来近似该算子。
- **关键技术细节**：
  - **DeepONet 架构**：包含一个分支网络（Branch Net）和一个主干网络（Trunk Net）。分支网络编码输入函数在固定采样点的值，输出系数向量 **b**；主干网络编码查询时间 t（或归一化时间 τ = t/Tf），输出特征向量 **φ**；最终预测为两者点积：û(t) = **b**(p)⊤ **φ**(t)。
  - **傅里叶增强（Fourier-DeepONet）**：在主干网络的输入中引入傅里叶特征（正弦/余弦函数），帮助模型捕捉长时域内的渐变和振荡。特征向量形式为 φ(τ) = [τ, sin(2πkτ), cos(2πkτ)]，k=1..K。
  - **训练数据生成**：通过直接法将连续时间最优控制问题转化为有限维 NLP（使用 SQP 求解器 SLSQP），在多个随机生成的实例上求解得到最优温度曲线 T*(t) 及其对应的输出曲线。训练数据为 {p_i, (t_ij, T*_i(t_ij))}。
  - **损失函数**：均方误差（MSE）或平均绝对误差（MAE），逐点回归损失。

- **算法流程（文字说明）**：
  1. 生成 N 个实例，每个实例包含随机采样的期望分子量曲线和最终转化率。
  2. 对每个实例，求解动态优化问题（SQP 多起点）获得最优温度曲线，记录时间网格上的值。
  3. 将输入曲线在固定时间点采样，与标量参数拼接形成分支网络输入向量；主干网络输入为归一化时间（可选加傅里叶特征）。
  4. 训练 DeepONet 模型（最小化平均平方误差），学习从输入向量到温度值的映射。
  5. 对于新实例，只需前向传播即可得到任意时间点的近似最优温度。

## 3. 实验设计

- **数据集/场景**：案例为甲基丙烯酸甲酯（MMA）批处理聚合反应器。每个实例定义两个期望轨迹（数均分子量 Mn(t) 和重均分子量 Mw(t)）以及标量最终转化率 X(Tf)。输出为最优温度 T*(t)。实验使用总事例数 N（未明确数值，但固定 20% 为测试集）。
- **Benchmark**：动态优化问题的精准解（由 SLSQP 求解器得到的 T*(t)）作为真值。
- **对比方法**：
  - Plain DeepONet A（较小）
  - Plain DeepONet B（较宽，参数更多）
  - **Fourier DeepONet**（傅里叶增强）
  - 全连接前馈网络（FNN 基线，直接映射输入向量到离散温度向量）
- **实验设置**：
  - 全视野预测（80%训练，20%测试）
  - 数据效率（训练比例降为 60%、40%）
  - 随机水平截断（每个实例随机截断到 ri ∈ [0.5,1.0] 的上限，模拟移动水平）

## 4. 资源与算力

- **文中未明确说明**所使用的 GPU 型号、数量、训练时长、总参数规模等具体算力信息。
- 仅提及使用 Adam 优化器（学习率 10⁻³）训练固定迭代次数，所有模型基于 DeepXDE 库实现。
- 需要指出：缺乏算力细节限制了实验的可复现性。

## 5. 实验数量与充分性

- **实验组数**：三类实验（全视野、数据效率、随机截断），每类报告 MAE 和 MSE 表，并展示了三个测试案例的可视化曲线。数据效率实验包含 80%、60%、40% 三种训练比例。
- **充分性评估**：
  - 对比了不同架构（大小、傅里叶特征）和基线（FNN），参数规模大致相当，公平性较好。
  - 数据效率实验揭示了不同模型在少样本下的泛化能力。
  - 随机截断实验验证了坐标型算子模型的优势（FNN 无法处理变长输出）。
  - **不足之处**：仅在一个案例（MMA 聚合）上验证，泛化性受限；未与更现代神经算子（如 FNO、Transformer-based 算子）对比；未进行消融实验（如去掉傅里叶特征的效果）；未报告多次重复实验的统计结果（可能只有一次运行）。

## 6. 论文的主要结论与发现

- 傅里叶增强 DeepONet 在所有设置下均显著优于 Plain DeepONet 和 FNN 基线，MAE 和 MSE 降低 50% 以上。
- 单纯增加 Plain DeepONet 的宽度（更多参数）不能可靠改善预测精度，而引入傅里叶特征更有效。
- 在数据减少时，傅里叶 DeepONet 退化更慢，数据效率更高。
- 在随机水平截断场景下，傅里叶 DeepONet 保持低误差，而 FNN 大幅退化，表明坐标型算子模型在可变时间范围泛化方面具有天然优势。
- 结论：深度神经算子可有效近似轨迹优化问题的解，傅里叶增强结构显著提升精度与泛化能力。

## 7. 优点

- **方法创新**：首次将 DeepONet 直接用于学习动态优化问题的解算子，而非传统“代理模型+优化器”的两段式方法。
- **傅里叶特征改进**：在 trunk 网络中加入正弦/余弦特征，缓解了标准 DeepONet 在学习长时域形状上的困难。
- **实验设计全面**：涵盖全视野、数据效率、变水平三个角度，验证了鲁棒性和实际应用场景（如移动水平控制）。
- **公平对比**：保持参数规模可比，区分了参数增加与特征改进的贡献。
- **代码开源**（基于 DeepXDE），利于复现。

## 8. 不足与局限

- **案例单一**：仅在 MMA 聚合反应器上验证，缺乏对不同动态系统（如生物反应器、连续过程）的测试，结论泛化性有限。
- **未与最新方法对比**：未与傅里叶神经算子（FNO）、图神经算子、Transformer 等更先进算子架构比较。
- **训练数据依赖昂贵优化求解**：生成标签需要求解大量动态优化问题（多起点 SQP），本身计算成本依然较高，仅当标签可离线预计算时才有意义。
- **无消融实验**：未单独剔除傅里叶特征或改变分支网络编码方式，无法完全归因性能提升来源。
- **算力资源不明确**：缺失 GPU、训练时间等信息，影响复现与可扩展性评估。
- **未分析实时性**：虽宣称可用于在线决策，但未测量模型推理时间或与求解器的时间对比。
- **可能偏差风险**：固定测试集 20%，但总实例数 N 未给出，若 N 较小则测试集方差可能较大。

（完）
